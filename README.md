Sources for the plugin `plugin.facebook.v4a`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.facebook.v4a" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
